# Prefer: copy repo root .env.example → .env
# (server auto-loads .env on start)

ADDR=:8080
# Leave empty to disable auth
API_KEY=
DB_PATH=./data/paopao.db
UPSTREAM_BASE=https://query.paopaodw.com
UPSTREAM_TIMEOUT_SEC=30
